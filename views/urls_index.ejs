<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
  <% include ./partials/_header %>
</head>
<body>

  <% if (!user) { %>
  <p>Please register or login first.</p>
<% } else { %>
 <br>
  <br>
  <table class="table">
    <thead>
    <% for (property in urls) { %>
      <tr>
      <td><%= urls[property].longURL %></td> 
      <td> <%= property %></td>
      <td><form method="POST" action="/urls/<%=property%>/delete"><input type="submit" value="DELETE" name="<%=property%>" class="btn btn-danger"></form></td>
      <td><a href="/urls/<%=property%>">Edit</a></td>
      </tr>
      <% } %>
    </thead>
  </table>

<% } %>

</body>
</html>